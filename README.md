# BurpSuiteFromScratch
#### run the project 
```bash
python3 proxyserver.py #start the proxy
python3 gui.py #start the web application 

# started the browser ( after configuring it to use the proxy )
# chromium case 
chromium --proxy-server="http://localhost:8888"
```